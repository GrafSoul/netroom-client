server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for any path — otherwise client-side routing breaks on F5
    location / {
        try_files $uri $uri/ /index.html;
    }
}
