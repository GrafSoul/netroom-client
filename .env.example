# ---- Клиент (Vite) ----
# Переменные для Vite ОБЯЗАТЕЛЬНО с префиксом VITE_ — иначе Vite их не отдаст в код.
# Внимание: VITE_-переменные попадают в собранный бандл и видны в браузере — секретов тут не место.

# Адрес API бэкенда (dev-vault-server слушает 3030)
VITE_API_URL=http://localhost:3030
